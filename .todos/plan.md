# TODO execution plan

> Generated by `/todo-plan` on 2026-08-11 — **do not edit by hand**, this file is regenerated from
> `.todos/*.md` on every run. Priority and batching live in the TODO files themselves.
>
> Open TODOs: 4 — Critical: 0 · High: 2 · Medium: 2 · Low: 0 · Locked: 0
> No lots this run: every open TODO uses a different batch key and none share a file path with a
> sibling — a standalone TODO is the normal case, run it in its own session, `/clear` between.

## Recommended order

| # | TODO | Priority | Batch | Prompt |
|---|------|----------|-------|--------|
| 1 | 0081 | High | blog-map | `/todo 0081` |
| 2 | 0084 | High | blog-palette | `/todo 0084` |
| 3 | 0083 | Medium | blog-questions | `/todo 0083` |
| 4 | 0085 | Medium | unassigned | `/todo 0085` |

## Lots

None this run — see *Standalone* below.

## Standalone

One `/todo NNN` each, in its own session — highest priority first. `0081` and `0083` both mention a
future `scripts/lib/` corpus loader in their own Notes sections, but neither declares it in `**Files**`
and both leave `**Depends**: —`, so they are not lotted on that basis — see the Anomalies note below.

| ID | Priority | Title | Batch | Why not lotted |
|----|----------|-------|-------|----------------|
| 0081 | High | Page `/map` : graphe interactif du corpus (248 posts, 25 séries) | blog-map | no file path shared with a same-batch sibling |
| 0084 | High | Palette de commandes `Cmd+K` : de moteur de recherche à shell du site | blog-palette | no file path shared with a same-batch sibling |
| 0083 | Medium | « Ask my blog » : index de questions généré par Ollama au build | blog-questions | no file path shared with a same-batch sibling |
| 0085 | Medium | BrowserWindow : seconde passe sur les captures restantes | unassigned | decision-only, no `**Files**` yet (`TBD`) |

## Anomalies

- `0081-blog-knowledge-graph-page.md` / `0083-ask-my-blog-question-index.md`: each TODO's own Notes
  section proposes extracting a shared `scripts/lib/` corpus loader, and each says to reuse it —
  but neither lists it under `**Files**`, and `**Depends**` is `—` on both, so today they read as
  independent. Whichever runs first should actually do the `scripts/lib/` extraction and the other
  should then declare `**Depends**` on it — until then this plan cannot order them relative to each
  other.
- `0085-browserwindow-seconde-passe.md`: `**Files**: TBD` — decision-only, correctly left out of
  every lot.
- `freshness-journal.md`, `reader-review-journal.md`: flat `.todos/*.md` files with no `# NNN — title`
  header — these are running journals (`/freshness`, `/reader_review`), not TODOs. Not part of the
  backlog; ignored by this plan, not an authoring bug.
