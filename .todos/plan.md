# TODO execution plan

> Generated by `/todo-plan` on 2026-08-22 — **do not edit by hand**, this file is regenerated from
> `.todos/*.md` on every run. Priority and batching live in the TODO files themselves.
>
> Open TODOs: 3 — Critical: 0 · High: 0 · Medium: 2 · Low: 1 · Locked: 0
> Batches marked `~` were inferred from `**Files**`, not declared.
> Lots require a shared path, never a shared theme — each lot names the directory its members
> share. A standalone TODO is the normal case: run it in its own session, `/clear` between.

## Recommended order

No lots this run — every open TODO is either decision-only or the sole member of its batch. See
*Standalone* below, highest priority first.

## Lots

*(none — see Standalone)*

## Standalone

One `/todo NNN` each, in its own session — highest priority first. These share no files with a
sibling (or declare none yet), so there is nothing for a lot to save.

| ID   | Priority | Title                                                       | Batch           | Why not lotted                                                      |
| ---- | -------- | ------------------------------------------------------------ | ---------------- | ---------------------------------------------------------------------- |
| 0091 | Medium   | Déploiement non atomique : aucun retour arrière possible    | deploy-pipeline  | only TODO in its batch (`.github/workflows/deploy.yml`)                |
| 0085 | Medium   | BrowserWindow : seconde passe sur les captures restantes    | unassigned       | decision-only, `**Files**: TBD`                                        |
| 0087 | Low      | « Try it here » : exécuter l'outil de l'article dans la page | blog-playground  | decision-only, `**Files**: TBD` — depends on a Go/No-go arbitration    |

## Partially done — remaining work

*(none — `PARTIAL/` no longer holds a TODO relevant to the flat backlog beyond what's listed
below)*

| ID   | Title                                                                        | File                                                                             |
| ---- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| 0088 | Commandes paramétrées : le lecteur saisit ses valeurs une fois, l'article s'adapte | [PARTIAL_0088-parameterized-commands.md](PARTIAL/PARTIAL_0088-parameterized-commands.md) — the retrofit batch (0104) is now closed DONE; the pilot itself was done from the start |

## Anomalies

- `0085-browserwindow-seconde-passe.md` and `0087-playground-try-it-here.md`: no `**Files**`
  (`TBD`) — decision-only, correctly left unlotted, but worth scoping before they're picked up so
  `/todo` doesn't have to derive scope blind.
