Il faut nettoyer le fichier `blog/tags.yml` qui n'est plus à jour car il contient environ 175 tags alors que le blog n'en utilise plus autant.

Il faudrait, peut-être, analyser chaque article depuis 2023; extraire la liste unique des tags et supprimer les tags qui sont dans `blog/tags.yml` qui ne se retrouvent pas dans la liste faite sur base des articles.
