$ docker run -d --rm \
    --name %%name=oracle-ords%% \
    --network oracle \
    -p %%port=8181%%:8181 \
    -e IGNORE_APEX=TRUE \
    -v ./ords_secrets/:/opt/oracle/variables \
    -v ords_config:/etc/ords/config/ \
    container-registry.oracle.com/database/ords-developer:latest
