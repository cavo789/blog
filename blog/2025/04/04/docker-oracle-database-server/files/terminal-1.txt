$ docker run -d \
    --name %%name=oracle-db%% \
    --network oracle \
    -p %%port=1521%%:1521 \
    -p %%port_oem=5500%%:5500 \
    -e ORACLE_SID=%%cdb=ORCLCDB%% \
    -e ORACLE_PDB=%%pdb=ORCLPDB1%% \
    -e ORACLE_PWD=admin \
    -v OracleDBData:/opt/oracle/oradata \
    -v ./scripts/startup/:/docker-entrypoint-initdb.d/startup \
    container-registry.oracle.com/database/enterprise:latest
