name: demo

services:

  %%name=counter%%:
    build: .
    container_name: %%name=counter%%
    image: demo/counter
    # highlight-next-line
    volumes:
      # highlight-next-line
      - counter_data:/data

# highlight-next-line
volumes:
  # highlight-next-line
  counter_data:
