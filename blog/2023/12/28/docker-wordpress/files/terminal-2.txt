$ docker rm $(docker stop $(docker ps -a -q --filter="name=%%app_name=app_wordpress%%"))

$ docker rm $(docker stop $(docker ps -a -q --filter="name=%%db_name=db_wordpress%%"))

$ docker network rm wordpress
