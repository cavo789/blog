services:
  app:
    build: .
    depends_on:
      - db
      - cache
    ports:
      - "8080:80"

  db:
    image: postgres:16
    volumes:
      - db-data:/var/lib/postgresql/data

  cache:
    image: redis:7

volumes:
  db-data:
