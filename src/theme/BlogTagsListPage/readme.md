# BlogTagsListPage

This directory contains a swizzled version of the `BlogTagsListPage` component from the Docusaurus theme.

## Purpose

It includes a patch to correct malformed tag permalinks that contain duplicate `/tags/tags/` segments.
